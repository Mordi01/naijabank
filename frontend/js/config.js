const CONFIG = {
    // For local testing:
    // API_BASE_URL: 'http://localhost:5000/api',
    
    // For production (we'll update this after Render deployment):
    API_BASE_URL: 'https://naijabank-api.onrender.com/api',
};